{
  "name": "servarr-stack",
  "version": "1.0.0",
  "private": false,
  "description": "Pulumi Kubernetes stack: servarr-stack",
  "type": "module",
  "scripts": {
    "up": "pulumi up --yes --stack ${PULUMI_STACK:-prod}",
    "preview": "pulumi preview --stack ${PULUMI_STACK:-prod}",
    "destroy": "pulumi destroy --yes --stack ${PULUMI_STACK:-prod}"
  },
  "dependencies": {
    "@pulumi/kubernetes": "^4",
    "@pulumi/pulumi": "^3",
    "yaml": "^2.8.1",
    "@pulumi/postgresql": "^3.15.3"
  },
  "devDependencies": {
    "@types/node": "^22.0.0"
  },
  "engines": {
    "bun": ">=1.1.0"
  }
}